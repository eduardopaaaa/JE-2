<!DOCTYPE html>
<html lang="en">
    <head>
        <%- include('partials/head.ejs') %>
        <link rel='stylesheet' href="https://fonts.googleapis.com/css2?family=Iceland&display=swap" rel="stylesheet">
        <style>
            body{font-family: "Iceland", cursive}
            </style>
    </head>
<body style= "background-color:whitesmoke";>
    <h1>Update Board</h1>
    <nav>
        <a href="/ws2/new"> Create New Item</a> 
        <a href="/ws2/home">| Home</a> 
    </nav>
    <table class="table">
        <% for (let i = 0; i < ws2.length; i++) { %>
        <tr>
            <% if (ws2[i].completed === false) { %>
                <td><%=ws2[i].priority %>
                    <td>ECD: <%= ws2[i].date %></td>
                    <td>Completed: <%=ws2[i].completed%></td>
                <td>
                    <a href="/ws2/<%=ws2[i].id%>">
                        Task: <%= ws2[i].name %>

                    </a>
                </td>
                <td>
                    <button class="btn btn-outline-primary"><a href="/ws2/<%=ws2[i].id%>/edit">Edit</a></button>
                </td>
                <td>
                    <form action="/ws2/<%=ws2[i].id%>?_method=DELETE" method="POST">
                        <input class="btn btn-danger" type="submit" value="DELETE" />
                    </form>
                </td>
            <% } else { %>
            <td><%= ws2[i].date %>
                <td><%=ws2[i].completed%></td>
            <td>
                    <%= ws2[i].name %>
            </td>
            <td>
                <button class="btn btn-outline-secondary disabled">Edit</button>
            </td>
            <td>
                <form action="/ws2/<%=ws2[i].id%>?_method=DELETE" method="POST">
                    <input class="btn btn-danger" type="submit" value="DELETE" />
                </form>
            </td>
            <% } %>
        
        </tr>
        <% } %>
    </table>
    
</body>
</html>